---
- hosts : all
  become : yes
  tasks : 
   - name : install webser on ubuntu node
     apt : 
       name : apache2
       state : present
   
   - name : i want to start a weber server 
     service  : 
       name :  apache2
       state : started   
      
   - name : i want to start a weber server on redhat nodes
     dnf : 
       name : httpd
       state : present

   - name : i want to start webserver on redhat nodes
     dnf :
      name : httpd
      state : started      
       